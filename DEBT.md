# DEBT — the consolidated open-work ledger (4W half)

> The p99.999 program (PRD-FIVE-NINES.md) re-prioritizes this ledger after
> each audit round; enrichment/coverage items below feed its workstreams.

*Created 2026-07-26 at the end of the owner-AFK stretch. One line per item,
with owner, source-of-record, and what resolves it. Items here are FILED, not
forgotten — each was deliberately deferred with a reason. When you take one,
delete its line in the same PR. S2W's half keeps its own ledger in
`data/name_shapes.yml` (`debt:` entries) and their NEGOTIATION turns.*

## Curation passes (renames/folds, per-family)

| item | source of record | what resolves it |
|---|---|---|
| Chevrolet `Lt`/`Ltz` family (9 records, uniformly wrong) | #78 dossier §6.4 | marque styling check (chevrolet.com trim pages) + one family pass; the `Hhr Lt` line was deliberately half-done to avoid a new contradiction |
| Chevrolet `Trail Blazer` vs `TrailBlazer` direction | #78 dossier §6.5 | GM styles it TrailBlazer; repointing the settled spaced lines is a direction change — needs its own reviewed pass |
| Chevrolet SS acronym family + S-10 family | #63 dossier (american tail) | the S-10 pass needs a live GM citation (brochure archive is gone); SS records now fixed per-record, the styling-pin question stays closed (blast radius) |
| Ferrari `Gta` residue (post-#36) — **FERRARI's, not Alfa Romeo's** | #78 dossier §6.1 · wave-5 italian dossier §E.9 | one key (`456 GT/Gta`) once the GTA badge is sourced. Re-measured 2026-08-01: the record is `car/ferrari/456-gt-gta`, display `"456 GT/Gta"`, `ca`+`us`, **0 registrations** (both sources are zero-count spec files), and the raws are literally `"456 GT/GTA"` (ca_nrcan) and `"Ferrari 456 GT/GTA"` (us_fueleconomy) — a slash-joined RANGE LABEL listing two models (NAMING §7.4), not a badge. The real cars already exist at `car/ferrari/456-gta` (nl, 4) and `car/ferrari/456gta` (nz, 3). Still open because both routes fail: ferrari.com's 456 page would source the GTA badge and serves an empty body, and a 0-registration record gives the live-registry measurement nothing to measure. A full-catalog sweep confirms this line is **not** Alfa Romeo's — there is no Alfa GTA record at all; Alfa's `"156 3.2 GTA"`/`"147 3.2 GTA"` strings exist only in raws and fold away before publication |
| Nissan `Sr` (4 records, uniformly wrong) | #78 dossier §6.2 | marque check; interacts with the aprilia/yamaha SR fixes (#79) — per-record, no pin |
| Volvo `Se` residue (4 records) | #78 dossier §6.3 | the SE pin (below) or four per-record lines |
| **`SE` styling pin** — the one pin worth doing | #78 dossier §7 | 63 records / 22 makes, all officially caps; costs 18 rekeys, all enumerated in the dossier; S4W-owned; ship as its own PR with every rekey in the same commit (the #71 lesson) |
| Jaguar Mk split family (`Mki`/`Mk 1` = one car at two live ids; `Mk 5/7/10` contradict source) | #63 dossier U1 | per-model split rule (Mark 1/2 arabic, VII–X Roman), needs its own pass |
| Daimler V8-250 inversion (source spells the car V8-250; five ids for one car) | #63 dossier U2, ready inversion block in-dossier | maintainer decision to reverse the settled line; inversion block is paste-ready |
| Morris `mini-mk-2` dangling (Austin and Morris now spell the same car differently) | #63 dossier U6 | would mint `morris/mini-mkii`; take it with the next Morris touch |
| Austin-Healey Mk family (~10 records: `3000-mk1`, `3000-mk`, `sprite-mk*`, `healy-mk3` typo) | #72 dossier §6 | one dedicated family pass; the `AUSTIN HEALEY` aliases fix (#72) already protects es rows |
| Mazda B-series displacement pooling (`B2000`/`B2500`/`B2600`/`B3000`/`B3000 FFV`/`B4000` → `B Series`) — WITHHELD, blocked by the source-forced-kind defect | wave-5 Mitsubishi+Mazda dossier §A.1/§D.5 judgment call 1; measured on a real build 2026-08-01 | the fold is well-evidenced (us_fueleconomy pools all 34 B-number rows onto ONE baseModel `B Pickup`) and it still must not ship as-is: pooling makes one id that is **99.11% van mass** (4,588 of 4,629 vehicles — uk_dft's own `MAZDA B SERIES` genmodel), so `Reconciler.cross_kind_prune!` correctly removes the pickup from the car kind — and `us_fueleconomy`, `ca_nrcan` and `nz_nzta` are all car-only sources (`def kinds = %i[car]`), so the ca/nz/us evidence on `car/mazda/b2000`, `b2600`, `b3000`, `b3000-ffv`, `b4000` has NOWHERE to land. Measured cost of taking it: **10 (country,source) pairs destroyed** and 5 dangling migrations. This is the already-filed *"Source-forced kind: us_fueleconomy/ca_nrcan hardcode kinds=[:car]"* item biting a curation change. **Unblocks when that item is fixed** (the EPA's own `VClass` column says `Standard Pickup Trucks 2WD` for exactly these rows), or if the ids are relocated by a reviewed kind-boundary pass. The E-series pooling was checked against the same arithmetic and is SAFE (van share 0.7472 — no prune), and it shipped |
| `mitsubishi` ↔ `mitsubishi-fuso`: the same Canter published twice in FOUR kinds | wave-5 Mitsubishi+Mazda dossier §A.4.1/§A.4.2 | a `moves.yml` question, REPORTED not applied, because it is not one-way: `truck/mitsubishi/canter` (es,fi,gb,lu,nl; 1,107 vehicles) vs `truck/mitsubishi-fuso/canter` (es,fi,gb,nl; 5,773) — pre-2003 Canters were badged MITSUBISHI and one moves line cannot split by year. Same pair in car/van/bus. Blocked siblings: `truck/mitsubishi-fuso/fe`, `van/mitsubishi-fuso/canter-3c13d`, `van/mitsubishi-fuso/canter-3c15` need the identical fold under a `Mitsubishi Fuso:` renames block that DOES NOT EXIST (the replay proves it: `ProbeRenames#[]` returned nil for every Mitsubishi Fuso row — 10 records / 6,371 vehicles never curated). Also `bus/mitsubishi/fuso` (nz, 455) and `car/mitsubishi/proton` (nz+ua, 8) — the make in the model column, no model token in the raw to move TO |
| Mazda `Xedos` bare range label (fi+gb, 259 vehicles; 252 are one uk_dft genmodel) | wave-5 Mitsubishi+Mazda dossier §A.7/§E.5 | the wave-5 pass proposed a `null` on the NAMING §7.6 BMW letter-label precedent and it was REFUSED at review: a null that deletes 259 real registrations with no honest target is not organize-never-delete. The id stays LIVE. What closes it is a DfT VEH0120 re-parse at the finer `Model` granularity (the same file whose 45 quarterly columns are already filed here), or a register carrying gb rows at row level; if either lands the correct action is a FOLD onto `xedos-6`/`xedos-9`, not a resurrection. Two routes already failed: majority-member (DfT has no displacement/body field, and neither Xedos id carries gb) and Traficom (7 rows are the bare string `XEDOS`) |
| Chrysler `300C: "300 C"` (settled line contradicts source; nl_rdw `300C` ×968) | #72 dossier §7 | 9-country repoint — needs its own reviewed pass |
| Datsun spaced Z-cars (`240Z: 240 Z` etc., generated boilerplate vs fused source) | #72 dossier §8 | re-source the five lines; `280ZX` (fixed) sits beside `280 Z` until then |
| FAW/Hongqi make attribution (`E-HS9` is a Hongqi) | #72 dossier §9 | `moves.yml` decision (`FAW\|E-HS9 → Hongqi\|E-HS9`), plus the hongqi-ehs7 sibling |
| VW alias-merge cluster (`beatle`/`bug`/`toureg`/`tranporter`…) | pipeline #30 PR body | wants MERGING not enriching; one dossier pass |
| Nissan 350Z/370Z lane-A (shipped keys are detector space-inserting artifacts) | #61 dossier | its own reviewed pass — do not drive-by reverse |
| ~40 unverified uniformly-wrong candidates (vespa GTS/GTV, ural CT, verge TS, triumph SD, yamaha CJ/NS/DA…) | #79 PR body | per-marque styling checks; the two-halves evidence rule is binding (cross-make attestation AND marque styling) |
| Maxus `E Terron 9` + `Mifa 9` casing residue (the marque writes `eTerron 9` and `MIFA 9` — https://en.saicmaxus.com/) | electric-van naming PR (2026-08-01) | same e-prefix ruling as eDeliver, but each moves an id (`e-terron-9`→`eterron-9`) and has candidate spellings to fold (`eterron9` nl 20, `eterron9-launch-edition`) — its own pass with its own aliases |
| **Saab `9-4X` / `9-7X` marque spelling** — Saab spells them CLOSED, the catalog publishes `9-4 X` / `9-7 X` while publishing its own `9-3X` closed | wave-7 Saab+Porsche+Bentley dossier §A.1.4b, paste-ready block in-dossier; re-measured on a real build 2026-08-01 | HELD, and it is not a rename: (1) it is an ALIAS INVERSION — `former_ids.yml` already ships `car/saab/9-4x → car/saab/9-4-x` and `car/saab/9-7x → car/saab/9-7-x`, so correcting the spelling makes an alias SOURCE a live id and the id-contract liveness gate rejects it outright; (2) renames are kind-blind and `Support.slugify("9.7X") == "9-7x"`, so closing the spelling merges fi_traficom's van row `Saab \| 9-7X` with nl_rdw's van row `SAAB \| 9.7X` and MINTS `van/saab/9-7x` for a full-size SUV. The apply re-measured the same hazard through a different door: a plain `"9-7": "9-7 X"` junk?-rescue key (2 car vehicles, fi+nl) also rescues an nl_rdw VAN row and mints `van/saab/9-7-x` — **car 27 + van 2 = grand 29, dominant share 0.931, below BOTH of `cross_kind_prune!`'s tests (`grand >= 100` and `share >= 0.97`)**, so the safety net cannot clean it up. A mint the pruner cannot catch is the thing to hold. Resolves with a reviewed pass that writes the alias inversion out and prevents the van mint |
| Four PRE-EXISTING retired Saab/Bentley slugs are still reachable from a raw spelling (`car/bentley/s-2` ← `S-2`/`S.2`, `car/bentley/s-3` ← `S.3`, `car/saab/9-7x` ← `9/7X`, `van/saab/9-7x` ← `9.7X`) | wave-7 apply, landing-slug replay over the whole corpus, all six kinds (2026-08-01) | 1 ROW each, so all four are below `publishable?` today and nothing republishes — but an unkeyed spelling holds a retired id open, and a second source arriving later resurrects it (the m-g-b rule forbids exactly that). Not fixed in wave 7 because `9-7x` is entangled with the held 9-4X/9-7X spelling above and `s-2`/`s-3` with the held Bentley S-cluster (U7); take them with whichever of those lands first |
| Mercedes-Benz `Eqt` unpinned while EQA/EQB/EQC/EQE/EQS/EQV are pinned (car/mercedes-benz/eqt, es+fi+lu+nl) | electric-van naming PR (2026-08-01) | one `EQT: EQT` styling line + a blast-radius sweep; deliberately not bundled with the e-prefix change |

## Structural / pipeline

| item | source of record | what resolves it |
|---|---|---|
| **A FOURTH SILENT-LOSS CLASS: folding onto a record at the 25-TAN cap silently destroys type approvals — and it is the only one of the four with a known population count.** `reconciler.rb:261` is `e.tans << row.tan if row.tan && e.tans.size < 25 # D7: TAN xrefs, capped`. When a fold retires an id whose rows carry TANs and the SURVIVOR is already at 25, the folded rows' approvals are dropped on the floor. **Population: 184 of 4,667 TAN-carrying records (3.94%) sit at the cap** — every one of them is a fold target that will eat evidence. **No instrument sees it**: the id diff is clean, the availability union is lossless at (country,SOURCE), `lint_overrides`/`lint_curation`/`reorg`/`lint_plates` are all silent, the build is green, and the survivor's `xrefs` array comes out BYTE-IDENTICAL to the control — the loss is visible only by diffing the RETIRED records' xrefs against the survivor's, which nothing does. Sits alongside the other three silent classes (inert keys; `test_override_key_reachability`'s kind-blindness; pre-rename-lookup `junk?` drops) and is worse than all of them in one respect: those hide work that never happened, this hides evidence that was published yesterday and is gone today | MINI §A.1 M3, measured control-vs-treatment 2026-08-02 (data#200 PR body). **How it was found, because the method generalizes:** the M3 fold's id diff came out *too clean* — `mini/cooper` byte-identical to the control apart from `popularity` — and a survivor that gains 670 vehicles and changes in no field is a contradiction, so I went looking for what a clean diff would hide. Concrete instance: folding `car/mini/jcw` + `car/mini/jcw-e` into `car/mini/cooper` delists 7 approvals — `e1*2018/858*00373*02..05` and `e1*2018/858*00347*02..04`, **0 of 7 surviving**. M3 was DECLINED by the owner on exactly this | Either (a) make the cap fold-aware / raise it (TANs are the cross-source join key, PRD-3 D7 — `pipeline#133` proved the Paceman/Aceman split partly on two approvals sitting under DIFFERENT EU framework directives), or (b) a gate that diffs retired-id `xrefs` against the successor's and fails on a non-empty set difference. (b) is the cheaper one and catches the class rather than one cap |
| **`nz_nzta` writes BODY WORDS in its MODEL column — the `pipeline#131` class, one register later.** Measured on the 2026-03 full-fleet snapshot: `COUPE` 1,839 veh across **57 makes**, `SEDAN` 616/52, `CONVERTIBLE` 280/15, `VAN` 32/10, `WAGON` 11/10, `HATCHBACK` 6/2, `LIFTBACK` 1/1 — plus MINI's `HATCH` 3,587, which data#200 fixes by curation for MINI only and therefore leaves the general defect in place (`Nissan/Hatch` 6, `Nissan/Hatchback` 5, `Vauxhall/Hatchback`, `Mazda/Hatchback`, `ZAZ/Hatchback` all still publish a body word as a nameplate) | MINI §A.1 M1 apply, all-six-kind corpus replay 2026-08-02 (data#200 PR body); precedent `pipeline#131` (fi_traficom body-word prefix — `car/porsche/coupe` was 251 Finnish 911s) | An adapter-level fix in `pipeline/sources/nz_nzta.rb`, and **IT MUST BE MAKE-AWARE — this is the whole difficulty and it is why the fix is filed rather than done.** `car/mini/convertible` is a REAL nameplate (BMW Group: "the MINI Convertible making its debut in spring 2004") and holds **219 of NZ's 280 bare `CONVERTIBLE` rows**; a blind body-word blocklist would destroy that record's NZ evidence outright. Same 21-KEEP shape the body-word dossier exists to prevent. Use `pipeline#131`'s method (census the register's own body vocabulary, then an EXPLICIT exclusion list with a measured reason per member) combined with `pipeline#128`'s make-aware token handling. NZ has no door-count prefix to census against, so the vocabulary has to come from the first-token distribution plus a per-make keep-list |
| U7 variant-strip Roman collapse (spaced Roman numerals stripped pre-rename → unresolvable truncated ids: `austin/healey-3000-mk`, `jaguar/mk` 5-country…). **Bentley is the same class, three clusters**: `mark`(48 veh)/`mk`(42)/`mk6`(16)/`mark-6`(11) + unpublished `mk-6`(11) are ONE car, the 1946–52 Mark VI (raws `MARK VI` ×36, `MK VI` ×27, `MK VI D.H.C.`); `s` pools `S II` ×5 and `S III` ×1 with genuine bare-`S` rows beside `s-type`/`s1`/`s2`/`s3`; `t` is `T` ×7 beside `t-series`/`t1`/`t2` | #63 dossier U7 · wave-7 Saab+Porsche+Bentley dossier §A.3.6/§E.7 (paste-ready Mark VI block in-dossier, measured green on a real build) | normalizer change with its own blast-radius measurement. NOTE the useful asymmetry the wave-7 pass measured: renames run AFTER the strip, so a rename TARGET can restore what the strip destroyed — the Mark VI cluster is therefore closable by curation alone (4 ids retired, `car/bentley/mark-vi` minted, fi+nl+nz) and was held only because DEBT says this family gets its own dedicated pass. The S and T clusters are NOT closable that way: folding bare `S`/`T` would merge real S1/S2/S3 and T1/T2 evidence into a stub |
| `VARIANT_SUFFIXES` `.*` tails destroy model info (`VW KOMBI 1500` ≠ `VW VARIANT 1600`) | #78 dossier §3 | normalizer design question, not curation |
| Post-release fold-key cleanup habit | #70 precedent (double-space keys), pipeline #31/#33 | at each release: prune rename keys and enrich twins whose producing raws/ids died — the insurance lint (#34) now catches the enrich half at PR time |
| Kind-boundary proposal (van/411 is a car; t2–t6/lt/vanagon in car kind) | `PROPOSAL-kind-boundary.md` + pipeline #30 PR body | owner review of the proposal |
| **`VARIANT_SUFFIXES[0]` contains the token `GTC`, and it erases a CURRENT, ON-SALE Bentley model line.** The token was added for the Opel Astra GTC (a three-door body). It fires on ` GTC…` and deletes the rest of the string, so `CONTINENTAL GTC SPEED` arrives at the rename site as the eleven characters `Continental`. Measured on a per-row replay of pipeline `origin/main`: **479 vehicles across ca/es/fi/lu/nl/th/ua/us** (CORRECTED 2026-08-01 by the batch-item-4 raw replay: the original said "≈460 across ca/de/es/lu/nl/th/ua" — that country list was `car/bentley/continental`s AVAILABILITY, not the GTC rows countries. `de` has ZERO GTC rows and `fi` (9) was missing. Reading the right record with the wrong denominator.) — `BENTLEY CONTINENTAL GTC` ×171, `CONTINENTAL GTC SPEED` ×84, `BENTLEY CONTINENTAL GTC V8` ×69, `CONTINENTAL GTC` ×56, `CONTINENTAL GTC MULLINER` ×17 and eleven more spellings (17 in total, not twelve) — **all land on `car/bentley/continental`, and the Bentley Continental GTC has no record at all**. `Continental GT` survives only because `GT` is not in that token list | wave-7 Saab+Porsche+Bentley dossier §A.3.1/§E.5 | a normalizer change making the `GTC` token make-aware or position-aware, with a full blast-radius measurement on Opel (the token's original customer) and a control build. **Curation CANNOT fix it and the wave-7 pass did not try**: the probed rename key is `Continental` (verified byte-exact), i.e. the GTC is gone *before* renames run, and a `Continental` key would swallow 17,080 gb Continentals with it. Same rule-first class as the Ford `CARAVAN` defect |
| **`test_override_key_reachability` has TWO independent blind spots and is now auto-passing car keys.** (1) It accepts a key if ANY kind reproduces it — and the motorcycle/moped light path reproduces car strings verbatim, so `Continental GTC Speed` reads "reachable" for a make (Bentley) that has no motorcycles. **Any car rename key that survives the 2W light path unchanged is auto-reachable**, which is most of them. (2) It measures with renames DISABLED, which inverts the ORDER FIX for rescue keys — two live Austin-Healey keys read dead under it (filed separately, fixed as pipeline#106). The instrument we lean on to prove a key is not inert has been settled by build diffs twice this week instead: the Land Rover keys passed it while being inert (caught by the duplicate sweep), and five of the DAF power-suffix keys silently did nothing while it stayed green (caught by a control-vs-treatment id diff) | batch item 4 raw replay (2026-08-01) established (1) by checking rather than asserting; (2) is the Austin-Healey case from the wave-8 window; the DAF chain case is data#176 | a key-liveness check that actually binds: assert per-KIND reachability for the kind the key is written for, and run it with renames ENABLED (the ORDER FIX order). Until then **the control-build id diff is the real instrument and the test is a smoke check** — say so wherever a fold checklist cites it |
| **`report:junk_drops` is BLIND to the class it exists to find.** `report_junk_drops.rb` returns early for rows that died BEFORE the rename lookup, so any defect that kills a row inside `classify`'s own guards is invisible to the tool built to surface deleted nameplates. Measured 2026-08-02 on smart's `#1`/`#3`/`#5`: the fix moved `vehicles_dropped_by_classify` 3,643,960 → 3,640,933 (**−3,027 exactly**) while `vehicles_dying_in_junk` stayed **unchanged** at 264,880 — 3,027 real vehicles were being deleted in a place the drop report never looked. This is the SECOND instrument shown blind in one night, alongside `test_override_key_reachability` (row above); between them they are the two checks most often cited as proof that a key is live and that nothing is being deleted, and neither binds. | own measurement, batch item 9 (`pipeline#134`) control-vs-treatment plus the report's own before/after | report the pre-rename-lookup drops as their own class instead of returning early — the count is already computed (`vehicles_dropped_by_classify`), it is simply not attributed to a nameplate. Until then, treat a clean `junk_drops` as evidence about the `junk?` predicate ONLY, never as "nothing is being deleted" |
| **The fake BODY-WORD nameplate class is catalog-wide, and the ADAPTER-STRIP remedy this row used to prescribe is REFUTED outside Finland.** 92 published car ids are named after a body word (`car/mercedes-benz/sedan`, `car/cadillac/coupe`, `car/ford/saloon`, `car/porsche/coupe`, `car/bentley/saloon` …). RE-MEASURED 2026-08-02 on a green control build (`f9d5637`, frozen cache) with the CLOSED 13-word vocabulary (`coupe roadster sedan saloon convertible cabriolet limousine cabrio estate hardtop mpv wagon van` — a looser one gives 132 ids, a different class): still **92**, per source nl_rdw 84 · nz_nzta 82 · fi_traficom **26** · uk_dft 14 · us_fueleconomy 12 · ua_mvs 10 · es_dgt 7 · lu_snca 6 · ca_nrcan 5 · th_dlt 2 · my_jpj 1. Disposition **21 KEEP · 3 FOLD · 68 NULL**. The 21 KEEPs are **49,837 vehicles of real nameplate** a blind sweep destroys (`audi/coupe`, `fiat/coupe`, `mazda/mpv`, `mini/convertible`, `smart/roadster`, `tesla/roadster` …); the 68 NULLs are **2,385 vehicles** with no honest successor. `truck/mercedes-benz/umpikorinen` is OUTSIDE this vocabulary and is a separate fi single-source leftover. Outside `car` the class is 4 ids, 3 of them real 2W nameplates — there is no cross-kind sweep to plan. | own re-measure 2026-08-02: control build at `f9d5637` (ALL GATES GREEN) + a whole-corpus replay of production `classify` with a recording proxy at the rename consultation site (`renames&.key?(nameplate)`), mechanisms active, nothing disabled; supersedes the 2026-08-01 enumeration, which predated `pipeline#131`/`data#183` | **CURATION, not an adapter strip.** The grammar measurement DEBT asked for was done (dossier `aux/research/2026-08-owner-swarm/bodyword-92-class.md` §C, on frozen caches): **neither nl_rdw nor nz_nzta has a body-first grammar** — no door count, no type code, no wheelbase tail. The body word is the WHOLE string in 227 nl pairs / 1,491 v and 281 nz pairs / 18,579 v, and where it is a prefix it is overwhelmingly part of a real nameplate. A Finland-shaped strip there rescues **401 vehicles across nl+nz** while putting **12,200 vehicles of real nameplate** at risk behind a pin list longer than `#131`'s entire vocabulary — the inverse of Finland's 2,244-rescued / 136-pinned. So: **NO strip for nl_rdw, nz_nzta, uk_dft, us_fueleconomy or ua_mvs.** The remedy is per-id `renames.yml` `null` keys + `removals.yml` entries, keyed on the string the pipeline PROBES (collapse_variant runs first — the dossier's §B.3 residue keys are drafted against RAW strings and are all unreachable/inert), with the KEEP list protected and each NULL carrying its register, string and count. `data#209` ships 59 of the 68; 9 were deferred (see that PR: 8 to make-owning siblings, `lancia/coupe` REFUTED). **`lancia/coupe` is now CLOSED as a FOLD, not a NULL and not a KEEP — so the class disposition is 21 KEEP · 4 FOLD · 67 NULL, and 8 remain deferred.** Its 79 v (gb 61 + nz 18) are Lancia Beta Coupés and land on the live `car/lancia/beta`; the *mechanism* filed here was also wrong and is corrected in place: they are **NOT** recoverable through the uk_dft `Model` column — those three Model strings (`COUPE 2000 VX`, `COUPE IE 2000`, `COUPE VOLUMEX`) classify to `Coupe 2000 Vx`/`Coupe Ie 2000`/`Coupe Volumex`, none live, so an `FW_POOLS` entry would MINT THREE IDS. The instrument is a make-scoped rename on the GenModel-derived string (`Lancia: Coupe: Beta`), which also carries the nz half the pool could never reach. **And it refutes this row's own "uk_dft is the KEEP-DETECTOR" claim for PRE-1990 rows**: the same `GenModel` column carries `LANCIA BERLINA` 5, `LANCIA CPE` 39, `LANCIA HF` 5 and `LANCIA SPYDER` 17 beside `LANCIA BETA` 494 — a body word, an abbreviation and a trim letter-pair cannot all be manufacturer-declared families, so for that era the column is a hand-keyed fragment field that splits ONE nameplate nine ways. Treat the KEEP-detector property as MODERN-ROWS-ONLY. uk_dft is the class's KEEP-DETECTOR, not a defect source: 13 of its 14 body-word ids are confirmed real by its own `GenModel` column |
| **An adapter-level body/door prefix strip CANNOT be verified from candidates — only a published-id diff sees it — and the `<door><BODY>` half is REFUTED at that level.** Widening the fi body-first strip to `<door><BODY> <model>` ("2DSEDAN 96-V4/2500", "4D SEDAN 9-5 2.3-E-48E/270") was built and diffed against a frozen-cache control: it moves 5,158 rows / 19,518 vehicles onto **326 existing records** — a real rescue, Mercedes C-Class +1,430, E-Class +895, BMW 3-Series +804, Audi A4 +382 — and it **PUBLISHES 118 new ids**, 105 of them type-approval strings (`car/volvo/p-44403`, `car/peugeot/504-a11`, `car/mercedes-benz/250-se-automatic`, THREE ids for one Trabant P601, two of which collide with existing `former_ids` aliases and fail the id-contract liveness gate). **The trap: none of them clears the single-source volume floor** (`KIND_THRESHOLDS[:car]` = 1,000; the largest is 239 vehicles). They publish through `publishable?`'s FIRST arm, `e.sources.size >= 2`, because nl_rdw and nz_nzta carry the same chassis strings for the same 1960s cars — two registers agreeing on a type code is corroboration by the letter of the rule and garbage by the meaning of it. A candidate-level or MIN_VEH-level simulation reports this as safe. It is not | own control-vs-treatment build, pipeline `2511322` + data `4ff4124`, frozen cache, 2026-08-01 (fi body-prefix PR) | the door-count class already has a shipped remedy that mints nothing (data#179, 1,011 keys / 11,169 vehicles). Reopening the adapter route needs (a) a residue test that can tell `944` from `280` — which is catalog knowledge the adapter does not have — or (b) a corroboration rule that does not count two registers quoting the same type code as two independent sources. **Re-measure the FILED figures too**: this pass measured the Saab+Porsche+Bentley body/door-prefix loss at **584 vehicles** (Saab 523, Porsche 50, Bentley 11), not the filed 760 — data#179 had already rescued ~188 Saab vehicles — while the filed "251 Finnish vehicles inside `car/porsche/coupe`" reproduced EXACTLY |
| **`report:junk_drops` HAS A SECOND, UNDOCUMENTED BLIND SPOT, and it is the CORROBORATION GATE — a country covered by exactly one register can lose its ENTIRE national fleet invisibly.** The tool documents one gap ("all three join rules need a live sibling"). That is not what happened here. `candidates` runs `next if v[:sources].size < 2 \|\| v[:countries].size < 2` two lines before the join rules ever fire, so ANY single-country nameplate is discarded however large. **The demonstration, measured on my own per-row replay of pipeline `origin/main` (2d2f160) over 378,275 corpus rows, 2026-08-01: `junk?`'s parenthetical rule kills 3,024 (make,key) pairs / 75,923 vehicles across 275 makes, and BYD alone is 20,298 of them — 26.7%, the #1 make in the catalog for that rule, ahead of Yamaha's 13,843. `report:junk_drops` reports ZERO rows for BYD, in BOTH its lists.** Every BYD casualty is `th_dlt`-only (`Dolphin (435KM-Std)` 10,233 · `ATTO3 (410KM-Premium)` 4,596 · `ATTO3 (480KM-Ext)` 3,266 · `Dolphin (490KM-Ext)` 2,144 · `ATTO3 (410KM-Dynamic)` 51 · `Seal U (dmi-Premium)` 7 · `Atto 3 (410KM)` 1), and every one of them HAS a live sibling — `car/byd/dolphin`, `car/byd/atto-3` and `car/byd/seal-u` all exist, so `R3-EXACT` would have fired on all seven. The single-source countries this hides are every non-EU market the catalog has: `th`, `my`, `ar`, `ua`, `nz`, `ie`. **Cost while it was invisible: `car/byd/dolphin` published de,es,fi,gb,ie,lu,my,nl,nz,ua and NO `th` while 12,377 Thai Dolphins were deleted on every build** (fixed by curation in the wave-8 apply, but the DETECTOR is still blind to the next one) | wave-8 Pontiac+Aston Martin+BYD dossier §E.2/§D.7, re-measured by the apply on its own replay; the tool is `pipeline/tools/report_junk_drops.rb`, shipped 2026-08-01 | a third list in the tool — "single-source drops above a HIGHER floor with a live sibling" — or relaxing the gate to `sources.size >= 2 \|\| vehicles >= 10 * MIN_VEH`. Either would have surfaced `BYD "Dolphin (435KM-Std)" — 10,233 vehicles` as the largest rescue candidate in the catalog. **Two routes that do NOT work and were checked: raising `MIN_VEH` does nothing (the gate is on source/country COUNT, not volume), and the `unjoined` list is stricter still (`>=3` sources AND `>=3` countries).** The tool's header comment has been extended in the wave-8 pipeline PR to say so; the code change is this line |
| **`junk?`'s litre rule (`/\b\d\.\d\b/`) costs the Saab 9000 ~300 vehicles that curation cannot reach economically.** `9000 CS 2.0 ECOPOWER` ×75, `9000 CS 2.3 ECOPOWER` ×75, `9000 CS AERO 2.3 TURBO` ×41, `9000 CS 2.3 TURBO` ×18, `9000 CST AERO 2.3 E2` ×17 … ~40 distinct keys, all `9000 <body> <litres> <trim>`, all nl_rdw, all real Saab 9000s. (The *nameplate* half of the same rule — `9.3`/`9.5`, where a decimal IS the name — is fixed at the curation layer in wave 7, because renames run before `junk?`.) | wave-7 Saab+Porsche+Bentley dossier §E.2 | a normalizer change: the discriminating fact is that the litre figure is **not the first token** and the prefix (`9000`) resolves to a live nameplate for that make. Rule-first per NAMING §4 (~40 lines is over the threshold), and it needs a control build plus a `report:junk_drops` confirmation to the vehicle — DEBT's own simulation of this family minted 6,621 garbage nameplates when written blind. A `drop_patterns.yml` entry is the wrong direction: that file *drops*, it cannot *rescue* |
| **`junk?` deletes W. O. Bentley's nameplates, which ARE litre figures.** Pre-war Bentleys are *named* by displacement — the 3 Litre, 4½ Litre, 6½ Litre, 8 Litre, and the Derby-era 3½ and 4¼ Litre. The litre rule kills `3.5 Litre` (8 veh), `4.5`, `4.5 Litre`, `6.5 Litre`, `4.4 Litre`, `Derby 3.5 Litre`, `Sportscar 8.0 Litre`; the letterless rule kills `4 1/4` (9 veh), `6 1/2`, `41/4`; and the `?` rule kills `4 ? Litre` (a mojibaked `½`). **≈33 vehicles of real pre-war Bentleys across nl+nz+es never become records at all**, and `car/bentley/3-litre` survives only because "3 Litre" has no decimal point. This is the car-kind counterpart of NAMING §6's motorcycle exemption ("a decimal in a two-wheeler name is part of the nameplate, never a displacement") — Bentley is the make that proves the car kind needs one too | wave-7 Saab+Porsche+Bentley dossier §E.11/§C.3 | deliberately NOT rescued in wave 7: every rescue MINTS a new nameplate, which is the class DEBT warns about after simulating the door-count fix, and it first needs a canonical-spelling decision (`4½ Litre` and `4 1/2 Litre` both slugify to `4-1-2-litre` through NFKD; Bentley writes `4½-Litre`). Belongs to the *Bentley heritage* pass — the wave-7 dossier §C.3 shows every unresolved Bentley question is an era-1/era-2 question and none is a Continental-GT-era one, which is a clean split for a follow-up |
| uk_dft is read at `GenModel` altitude, which POOLS distinct products: `MAXUS DELIVER` (van/maxus/deliver, gb 25,660) is measurably ≥6 products — the same file's `Model` column splits it into DELIVER 9 / DELIVER 9 LUX / DELIVER 7 / E DELIVER 3 / E DELIVER 5 / E DELIVER 7 / E DELIVER 9. **LARGEST INSTANCES YET, 2026-08-01 (wave-8 apply, re-measured from `cache/uk_veh0120_uk.csv` directly, not inherited): `BYD SEAL DESIGN EV` = 50,601 gb vehicles holding THREE nameplates — Seal U 26,923 (`SEAL U BOOST DM-I PHEV AUTO` 13,547 + `SEAL U COMFORT DM-I PHEV AUTO` 10,762 + `SEAL U DESIGN DM-I PHEV AWD A` 2,614), Seal 15,204 (`SEAL EXCELLENCE EV AWD` 10,534 + `SEAL DESIGN EV` 4,670) and Sealion 7 8,474 (`SEALION 7 COMFORT EV` 3,640 + `SEALION 7 EXCELLENCE EV AWD` 3,070 + `SEALION 7 DESIGN EV AWD` 1,764). There is NO `BYD SEAL`, `BYD SEAL U` or `BYD SEALION 7` GenModel in the file at all, so all three of those live nameplates publish today with NO `gb` — in the market where BYD sells most of them. `BYD DOLPHIN` = 6,131 of which 5,749 are DOLPHIN SURF rows and only 382 are Dolphins. `ASTON MARTIN DB1` = 845 of which 844 are DB12s (`DB12 V8 AUTO` 523 + `DB12 V8 VOLANTE AUTO` 321) and exactly ONE is a real DB1 — a nameplate of which fifteen were built.** | electric-van naming PR (2026-08-01), measured on cache/uk_veh0120_uk.csv; wave-8 Pontiac+Aston Martin+BYD dossier §E.1 + apply re-measurement | a uk_dft adapter change (read `Model`, keep `GenModel` as the fallback) with its own blast radius — it is a strict availability GAIN (gb joins six real Maxus ids, and three real BYD ids). NOT a removals.yml case: honest targets exist, they are just in a column the adapter does not read, so retiring the pooled id would delete 25,660 (resp. 50,601) real GB vehicles to fix a display string. **CURATION CANNOT DO IT and wave 8 did not try**: a `renames.yml` key is 1:1 on the produced string, so `Seal Design EV: Seal` would move 35,397 vehicles of two OTHER nameplates onto `car/byd/seal`. **THE OBVIOUS SHAPE RULE IS REFUTED, measured**: "flag a bucket whose `Model` strings do not begin with the `GenModel`" returns 1,758 of 11,291 buckets / 5,607,443 of 46,456,654 vehicles (12.07%), led by `BMW / 3 SERIES` over `320D M SPORT AUTO` — which is CORRECT. What catches it is a CATALOG JOIN, exactly like `report_junk_drops.rb`'s `R3-EXACT`: flag a `(Make, BodyType, GenModel)` bucket whose `Model` strings resolve to more than one PUBLISHED nameplate of the same make. `SEAL U …` and `SEALION 7 …` are published BYD nameplates; `320D M SPORT` is not a published BMW nameplate. A contradicting record, not a shape |
| **BMW's M ids are the LARGEST measured instance of the uk_dft pooled-GenModel impurity — 34,164 gb vehicles, bigger than the BYD line above.** Filing this was a CONDITION of the owner's D-1 ratification ("ship despite the uk_dft impurity, **and file it** … add BMW's per-id impurity table to DEBT's existing uk_dft-GenModel line") and data#163 did not carry it out; the omission was caught by an adversarial audit of that PR, and the table below is re-derived independently from `cache/uk_veh0120_uk.csv` at index 6 (2026 Q1, Licensed + SORN), reproducing the dossier to within one vehicle. UK GenModels pool the M car with its M-Performance siblings and **no curation can split a pooled GenModel row** — the split happens above the register. Per id: `m2` 25,580 gb of which **15,710 (61.4%) is M240i + M235i** · `m3` 39,066 / 11,306 (28.9%, M340i+M340d) · `m4` 21,552 / 5,513 (25.6%) · `m8` 1,536 / 673 (43.8%, M850i) · `m6` 1,707 / 261 (15.3%) · `m5` 8,984 / 701 (7.8%). **`car/bmw/m2` publishes at gb rank 233, decile 2, with 61.4% of its gb mass being 2 Series cars.** And M2 is the one id where D-1 made gb attribution WORSE: before, 9,870 real M2s sat wrongly on `2-series`; after, 15,710 M235i/M240i sit wrongly on `m2` — 61.4% correct became 38.6% correct. The ratification's "+30,096 correctly placed" is a six-id aggregate that hides that sign flip | owner ratification of D-1 (NEGOTIATION); adversarial audit of pipeline#115/data#163, 2026-08-02; own re-derivation | Nothing to fix in curation — the fix is upstream (a Model-column join, as G-1 did for the 2W half) or a per-id `provenance` note in the emitted record. What was owed and is now paid is the DISCLOSURE: a consumer reading `car/bmw/m2` at decile 2 could not previously learn that most of its UK mass is not an M2 |
| **AN UPSTREAM RESULT CAP IS A SILENT SHORT ANSWER, AND ONLY TWO OF THE PIPELINE'S FOURTEEN SOURCES NOW CHECK FOR ONE.** The instance is closed (pipeline#156): `nz_nzta` chunks its ArcGIS group-by by MAKE first letter, two letters hold more (make,model) pairs than one chunk can carry, and PASSENGER CAR/VAN `C` and `M` came back at **exactly `maxRecordCount` (2,000) with `exceededTransferLimit: true`** — a flag that appeared **nowhere in the pipeline source**. Cost, measured same-vintage 2026-08-02 against the register's own count query: **60,795 NZ vehicles, 358 (make,model) pairs, 1 make (`MCRAE`)** absent from published data, and **every archived observation of both chunks is truncated — 2026-07-05 (the source's FIRST fetch) and 2026-08-01 — so NZ has never once published a correct car count.** The class is what stays open: a capped response is well-formed, parses, validates, and is simply *smaller than the truth*, so **no gate downstream of `fetch` can see it** — the id diff is clean, the delta gate is clean, the build is green. `VDB::Support.assert_not_truncated!` now covers the two cap shapes that exist today (ArcGIS `exceededTransferLimit`+cap, with pages reconciled against an independent count; `nl_rdw`'s Socrata `$limit`, raised from warn to raise). **The other twelve sources assert their own completeness with nothing at all** — if `uk_veh0120_uk.csv`, `ie_tem20.csv`, `my_cars_2026.csv` or a CKAN resource list ever arrives short, it publishes exactly the way NZ did. AUDITED CLEAN 2026-08-02, so nobody re-does it: nl_rdw's largest grouping is 202,878 of 500,000 (40.6%); `ar_dnrpa`/`ca_nrcan`/`ua_mvs`/`lu_snca` are CKAN `package_show` resource lists (10/16/16/225, not paged); no non-NZ cached payload carries a truncation flag or sits on a round number (`us_vehicles.csv` at 49,995 rows was checked *because* it looks like a 50,000 cap — it is a static EPA bulk file with no cap mechanism); and NZ's `A–Z`+`0–9` chunk alphabet was verified **complete** by a live count of makes starting outside it (**0 vehicles**) — the key's coverage was never the problem, only its capacity | own measurement 2026-08-02 on a frozen-cache control-vs-treatment build, BOTH ARMS GREEN (data `3948830` + pipeline `a759c31`; first measured at data `b5ad83e` + pipeline `0d9ed7d` and re-run at the newer base, identical NZ result), pipeline#156 PR body; flag reproduced in `archive/index.jsonl` back to the source's first fetch | **The generalisable remedy is a per-source conservation check, and one source already has one**: `de_kba_fz10` cross-checks its summed months against the register's own published YTD (`summed 1475091 vs published YTD 1475091, 0.0000% drift`) and would catch a short file immediately. Give every source that can express one an independent total to reconcile against — a register-published count, a row count from the portal's metadata, or a second endpoint — and fail the build on a mismatch. Sources whose upstream publishes no such total should say so in the adapter, so "unverifiable" is a stated property rather than an assumption |

## Enrichment (the big lever)

| item | source of record | what resolves it |
|---|---|---|
| **G26c Wikidata bulk import** — the next major enrichment program | PRD-QUALITY §14, PRD-PAID §2 | CC0 bulk source; staging + graduation through lint_enrich; conflicts lose to curation. Unbuilt. |
| G26d fueleconomy.gov per-year-per-trim specs | PRD-PAID §2 | spec'd, unbuilt; feeds catalog-plus |
| Coverage: 4W enrich sweep beyond the ~30 makes done | pipeline enrich/ (39 files, 506 ids) | continue the swarm pattern (research → verify → land) per marque |
| **`enrich/mini.yml` and `enrich/smart.yml` (both NEW FILES) — now UNBLOCKED and with a split disposition already worked out** | aux/research/2026-08-owner-swarm/mini-lexus-smart.md §B.1/§B.3; unblocked by the Paceman anchor (pipeline s4w/mini-paceman-anchor) | §B.1's `car/mini/aceman` block carried "DO NOT ATTACH UNTIL THE PACEMAN SPLIT LANDS". The split has landed, and the disposition measured on a real build is: **`car/mini/aceman` attaches LIVE** — the id neither moves nor is minted, it only loses `ca`/`us`, and lint_enrich reads liveness from the committed catalog where it is already live; **`car/mini/paceman` ships HELD (commented)**, because the anchor MINTS it and a minted id is neither live nor a pending-publish alias target under any merge order (the wave-7 saab/bentley + yamaha convention). Flip-back belongs to the release that publishes `mini/paceman`. Note for whoever takes §B.1: the dossier's `paceman` note says the CA sources hold 6 Paceman rows — a recount across BOTH NRCan files gives **16** (6 + 10), and the US figure of 24 is right |
| **`enrich` has no variant type for a GENERATION, so marque-documented generation codes are filed as `spelling` and silently dropped from the paid feed.** `emit.rb:479` excludes `type: spelling` by agreement (NEGOTIATION Turns 139/140: "register-artifact capture — misspellings, parser noise — never product facts"), which is right for `Boxter`/`Cayanne`/`365A`. But `enrich/skoda.yml` types the Octavia's VW-group generation labels `A5`/`A7`/`A8` as `spelling`, and the wave-7 Porsche pass followed that precedent for `Type 964`/`993`/`996`/`997`/`991`/`992`/`930`/`986`/`987` and the `356 A/B/C` series. **Measured on a real build: 15 of Porsche's 17 `spelling` entries are Porsche's own documented type numbers, not register noise, and none of them reaches `catalog-plus`** | wave-7 Saab+Porsche+Bentley apply (2026-08-01), measured on `build/out-private/catalog-plus`; precedent in `enrich/skoda.yml` | one catalog-wide decision, not a per-pass one — either add a `generation` variant type that emit keeps, or accept that generation codes live only in `note:` (they do today, and notes DO reach the private feed, so nothing is lost meanwhile). Wave 7 deliberately did NOT invent the type: two shipped passes disagreeing about the same axis is the "third improvisation" the market-name line above already warns about |

## From the baseline audit (2026-07-26 — see data/review/audit-v2026.07.5/RESULTS.md)

| item | source of record | what resolves it |
|---|---|---|
| **Truncation/family stubs** — the largest measured defect generator (setra/s, bmw/z-reihe 7,143 regs, bmw/i, volvo/b, ford/f, scania-vabis/l) | RESULTS §New-1 | detector (single-token id prefixing ≥2 live siblings) + the VARIANT_SUFFIXES/series-collapse normalizer cure above; folds with the disposition pair |
| tesla/y → model-y retirement (headline; wheelbase-proven) | RESULTS + verify-3 | fold+alias, one PR |
| Connector-merge detector (oder/ou/slash/space + make axis) | RESULTS §New-2 | extend D13's detector wordlist + make-axis pass |
| Converter-brand-as-nameplate (D5b) detector | RESULTS §New-3 | one-model-string-under-≥2-chassis-makes scan |
| TAN hygiene: strip `- (FMVSS)` literals (35 records, fi TAN column) + TAN-overlap caveat in NAMING §2 + a TAN-oracle duplicate detector | RESULTS §New-4 | pipeline fi_traficom fix + NAMING amendment + new detector |
| Source-forced kind: us_fueleconomy/ca_nrcan hardcode kinds=[:car] with unused class columns | RESULTS §New-5 | pipeline: map the class columns |
| Typo-split detector (edit-distance-1 in-make siblings) | RESULTS §New-6 | new detector; Spinter/Spriner/Srinter exemplars |
| Raw-layer fold detector (collisions in raws invisible in published names) | RESULTS §New-7 | detector over observed_variants |
| NISMO casing family + the 11 title-cased model-name initialisms (Atf/Dt/Sk/…) | verify-2, ledger-4 | per-make passes under the two-halves evidence rule |
| global_decile is an unweighted rank MEAN — variance-collapses broad records to mid-bands, inflates thin single-country ones (2W measured: 30.2% of 1-country records at d1; 7-country 0% at d1) | NEGOTIATION T129/T133/T137; PRD-FIVE-NINES §1.3.1 | reconciler: weight each country's decile by its model population; never a per-record certification filter meanwhile |
| Verdict-note consistency lint for audit ledgers (protocol v1.3 rule 12; the brixton case) | RESULTS-s2w §amendments | grep-able verdict-vs-note contradiction classes over data/review |
| us_fueleconomy `baseModel` column (col 66, never read) — a regulator's own model-vs-trim oracle, 340 Ford triples alone | Ford trim dossier §0 | pipeline: read it into a granularity oracle for trim-fold adjudication |
| ~~Three pre-existing dead rename keys~~ **THE "DEAD KEY" CLASS WAS A FALSE DIAGNOSIS — corrected 2026-08-01.** `propose_former_ids` reports a key as dead when the slug it targets is still live, but the slug can be kept alive by a DIFFERENT, UNKEYED spelling — so a firing key reads as dead. Measured on both: chrysler `Town&country Touring` **fires** (4 vehicles, nl_rdw ×3 + fi_traficom ×1) while `Town & Country Touring` (15 veh, unkeyed) holds the slug open; jaguar `Xj-Sc` **fires** on fi/nl/us rows and vanished from the report the moment the JLR wave keyed its sibling spelling. The remedy is therefore an ADD of the unkeyed siblings, never a re-key or a delete — and the survivor slug can receive many spellings (Town & Country's receives **seven**). Only chrysler's remains open, plus one genuinely dead key found by sweeping all 23: buick `120L`, whose target `120 L` is not live either | american wave-6 dossier; JLR wave-4 apply; propose_former_ids | ADD the unkeyed spellings (or display-pin them); fix buick `120L`. **And two method rules this cost us**: reachability must be counted in ROWS, not vehicles — `Chrysler "300C/SRT-8"` read dead because it fires through one `us_fueleconomy` row with count 0 — and a catalog-side tool cannot see a key that fires, for the same structural reason a dropped row leaves no record |
| Hygiene-2 normalizer queue (freeze lifted 2026-07-27): ~~AMG stub 74,727 regs w/ EPA targets~~ **CLOSED 2026-08-01 by the `mercedes()` sub-brand strip** (367 raws measured; 16,605 of the 74,570 replay-layer registrations re-homed onto 20 live nameplates, 0 ids minted, 0 ids retired). **The registration headline was misleading and is worth carrying forward:** 361/367 raws had a live home, but by REGISTRATION MASS 77% of the stub is a single uk_dft GenModel row, `MERCEDES AMG CLASS` (gb 57,573) — **CORRECTED by S4W the same night: it DOES decompose.** The uk_dft row carries a fourth column — `Cars,MERCEDES,MERCEDES AMG CLASS,AMG EQE 53 4M+ NGT ED HYPRSCRN,...` — and there are **198 distinct Model values** under that one GenModel, naming real nameplates in exactly the shape the AMG strip now handles (`AMG A 35 4MATIC AUTO`; `AMG GLC` x21, `AMG C` x20, `AMG GLE` x16, `AMG SL` x11). So this residue is **batch item 11, the uk_dft 4W Model-column detector** — the same move G-1 made for two-wheelers — and the AMG strip has just built its decoder. Two consequences: item 11 is no longer an abstract detector item, it is worth ~57.5k gb registrations on Mercedes alone; and item 11 must be measured with the AMG strip already landed, or its rescue will read smaller than it is. The stub stays live carrying the residue meanwhile · comma-decimal + 2.0T junk rules · hyundai family regex (\d0)(?!\d) · make-fragment strips LAND/MICRO/HD · per-kind rename scope (5 attestations) · single-digit junk rule · RDW decimal-comma · SPORT TOURER suffix · TRAFFIC/MASCOTT drops · Tesla quote-char · series_collapse multi-word first-token · 814D D-exemption | wave-3 dossiers, aux/research/trims-2026-07/ | one measured control build EACH; tight batching; folds keyed on today's strings are the fragile side |
| The #42 remainder: bare-letter pooling from HYPHENATED/pre-fused/slashed natives (stem rule `\A([A-Z]{2,})-?\d`) — 15 of #117's Group B are live pooling stubs; triage queue = 327 bare 1-2-letter candidates with ≥2 natives (true size 15<n<327: real pooling vs REAL two-letter nameplates (bmw/ix, lexus/lx) vs spelling seams — the shape cannot discriminate, per-id review required). Cheapest repro: truck/scania/lt, 23 vehicles | NEGOTIATION 182-183 | hygiene-2: per-id triage w/ G-5-style discipline, never a regex |
| **ROOT CAUSE CONFIRMED, REMEDY REFUTED — re-measured 2026-08-01 (batch item 6) against pipeline `60accdf` + data `37279e0`.** The mechanism is exactly as filed: `overrides.rb:53` merges `makes/search_aliases.yml` into `make_aliases`, and `normalizer.rb:431` derives `series_collapse`'s known-make word set from that merged map. **Three corrections to the rest of the row.** **(1) The word list is wrong.** Only **34** words enter the set from search aliases, **15 of them Latin-script** — BEEMER BIMMER CHEVY FUTURE GUZZI **HD** HISPANIA LAMBO **MB** MERC MH MONTESA STARK TRRS VDUB. `Alfa`, `Benz`, `Harley`, `VW`, `Fuso`, `Gasgas`, `Scutum`, `Motor` and six Cyrillic names are **also `makes/aliases.yml` keys**, so the proposed fix cannot remove them: four of the eleven words this row names are untouchable by its own remedy. **(2) The measured symptom no longer reproduces.** `truck/chevrolet/chevy-van` publishes correctly today via the wave-4 INTERIM keys (`renames.yml:1898-1899`, `former_ids.yml:4860`); there is no type-code nameplate left to fix. **(3) The one-line fix is NET HARMFUL.** Whole-corpus two-way replay (377,986 rows / 76,947,294 vehicles, `report_junk_drops.rb` method): **203 rows / 2,087 vehicles change, 100% of them truck+bus; car, van, motorcycle and moped are exactly 0.** Of that, **1,898 vehicles (91%) put a marketing nickname into the published nameplate** and 189 (9%) fuse a real designation. The reason is that registers write the nickname as a make prefix **in the model column** — `MB SPRINTER` under nine coachbuilder makes, `CHEVY VAN 20-…` under Chevrolet — which is precisely the dealer/JV double-branding case `series_collapse` rule #1 exists for, so the word set is load-bearing by accident but genuinely. Control-vs-treatment build (both against unmodified data main): **14 published ids retire, 12 mint, 15 new gate failures** — `truck/chevrolet/chevy-van` → `truck/chevrolet/chevy`; 7 coachbuilder `bus/*/sprinter` ids retire while 9 `*/mb-sprinter` mint; and **`bus/mercedes-benz/e15` + `e16` + `e17` + `e19` pool into one id named `Mb`**, which is **irreversible**: all four designations produce the identical string, so no rename key can split them back and no paired data PR can make the change whole. `car/chevrolet/chevy` also dies, via `cross_kind_prune!` (`reconciler.rb:118`) once truck mints the same id | own control build + whole-corpus replay, 2026-08-01 (batch item 6); Chevrolet wave-4 dossier §pipeline-findings for the original diagnosis | **DO NOT ship the derivation change.** Two of the 189 improved vehicles' cases are real and are reachable from the data side with zero blast radius: `bus/daf/200` is the **DAF MB200** bus chassis (https://en.wikipedia.org/wiki/DAF_MB200 — raws `MB 200 DKDL 564/600`, nl, 53 veh) and Mack's `MH 612`/`MH 613` are MH-series trucks; both want `renames.yml` keys, not a normalizer edit, and the DAF one needs care because renames are kind-blind and `truck/daf/230` also exists. The narrower normalizer rule the evidence actually supports — **do not strip a known-make word when the NEXT token is digit-initial**, which alone yields MB200/MH613/HD270 while keeping `Sprinter` and `E16` — touches all 324 ingest-derived make words and needs its own measured pass. **Verification note for whoever picks this up:** `test_override_key_reachability.rb` is KIND-BLIND (a key counts as reachable if any kind reproduces it) and the car path never calls `series_collapse`, so it stayed GREEN while this change silently killed two live rename keys. A kind-aware probe found them; the suite cannot |
| Increment 3's store is LOCAL-ONLY and unbacked: it writes to `archive/registrations/`, and `archive/` is gitignored as the raw-dump never-in-git zone (568 MB of register dumps, excluded for licensing under R3/R6). An asset whose whole premise is "history no one can recreate" currently survives exactly one `rm -rf` | pipeline#92 review, 2026-08-01 | OWNER DECISION: where the durable copy lives. Derived aggregate counts are already sold in the private layer, so committing them to the PRIVATE pipeline repo under a path outside the raw-dump rule looks consistent with existing posture — but it is a licensing call, not an engineering one. CI persistence is a second step (builds run in the data repo's CI against an ephemeral pipeline checkout) |
| `ca`/`us` publish `"ca": 0` / `"us": 0` in the private layer for 1,231 and 1,412 car records — a catalog adapter's `nil` becomes 0 via `counts[cc] += row.count.to_i`, so "not measured" renders as "zero vehicles" | pipeline#92 guard, first run | cosmetic in the private layer today and excluded from the archive on its declared basis; fix the nil-to-zero coercion at the source and let a catalog country that starts producing real counts fail the build (that guard already exists) |
| ~~`df_VEH0120_UK.csv` carries ~45 quarterly columns back to 2015 Q1 and `uk_dft` reads only the newest~~ **CLOSED by pipeline#148 (merged 2026-08-02 01:57 UTC as `ddc148f`) — and the count in this row was WRONG.** *Ledger note: `data#211` marked this row closed at 01:36 UTC, **21 minutes before** its pipeline half merged, so for that window this file asserted shipped work that was not shipped. `data#213` was opened to un-close it and is superseded — #148 landed first. Recorded because the ordering, not the outcome, is the defect: a companion DEBT PR must merge AFTER its pipeline half.* Measured on the cached file: **47 quarter columns, 2026 Q1 … 2014 Q3, no gaps** (53 columns = 6 metadata + 47 quarters), so **46** were being discarded, not ~45, and the floor is 2014 Q3, not 2015 Q1. DEBT:99's bmw/m1 correction already had this right; this row did not, and anything that had pinned a length off it would have stopped two quarters short — #148 derives the series from the header instead. **BASIS, settled from the file before any code:** the columns are **STOCK** (fleet licensed at each quarter end, Licensed + SORN — `COUNT_BASIS["gb"]`), never flow, so they belong in the append-only snapshot store as 46 more dated observations and **NOT** in `registrations-history.json`, whose `by_year` axis is `stock-survival` and which VEH0120 cannot feed at all (it carries no first-registration date). Four checks (all re-derived 2026-08-02 on pipeline `bc1ae57` + data `c842fdb` by a script loading no pipeline code): every column totals between **39,343,783** (2014 Q4) and **49,647,017** (2026 Q1) against ~2M/yr UK new registrations; `ROVER METRO` (production ended 1997) decays 4,455→2,921; `TESLA MODEL 3` is 0 in all 19 columns before 2019 Q2 then accumulates 173→139,524, strictly increasing in every one of the 27 steps; and the two `LicenceStatus` halves **swap in mirror image on the winter boundary** — SORN rises into Q4 in **12 of 12** steps (median +338,999) while Licensed falls into Q4 in **11 of 12** (median −187,670), and spring reverses it (Licensed rises into Q2 in 11 of 11, SORN falls in 9 of 11). **CORRECTION 2026-08-02: this row and pipeline#148's body both originally said "SORN dips every Q3, rises every Q4/Q1". That is backwards and is retracted** — measured, the step into Q3 is a RISE in 10 of 11 and the step into Q1 is 6 up / 6 down, i.e. no signal; only the Q4 half was right. The conclusion (a point-in-time census, not a period total) survives and is better supported by the mirrored swap than by the sentence that was written, but the stated evidence did not survive. The 47 columns sum to **2,109,041,863** at raw-file level (the 46 historic ones to 2,059,394,846), i.e. **~42×** the UK fleet — not the "1.89bn, ~50×" first filed here, which mixed the published-record-level figure into a raw-file claim — and the sum is meaningless by construction — emit now REFUSES a series from any flow-basis country so the distinction is structural | pipeline#92 §free-finding; own replay of the DfT cache 2026-08-02; pipeline#148 | **Shipped: 46 observations × 4,002 records = 184,092 NEW dated (record,quarter) points, bringing the `uk_dft` store to 188,094 held in total; measured 912 KiB → 7,124 KiB, i.e. +6.07 MiB; parse cost only — no new fetch, no new cache byte, no new licence question (OGL v3 covers all columns).** Public catalog byte-identical — **re-proved from scratch on the merged base** against a pristine detached control at `bc1ae57`: all six kinds **+0/−0 ids and identical id ORDER** (so rank and decile did not move either); **all 13 files under `catalog/` byte-identical**, as are all five under `dist/` (including `catalog.sqlite` and `vehicles.parquet`) and `ATTRIBUTION.md`; identical file inventory; whole `build/out` identical but for `manifest.json`'s `built_at` wall-clock stamp. Control and treatment produced the SAME 5 gate failures (#147's xref-loss gate on five `truck/daf/xf*` folds, pre-existing on `main`) — this change added zero. The `2026-Q1` store file kept its digest and its bytes; all ten other sources' store files are byte-identical; a second build at a different `VDB_VERSION` reported `57 unchanged` and left the store byte-identical, so the backfill is idempotent as designed. `cache/` is untouched: a sha256 manifest of all 228 cached files is identical before and after, `uk_veh0120_uk.csv` still hashes to the vintage `VEH0120_CORPUS_SHA` pins, and `rake test` therefore ran with **0 skips** — the pinned AMG known-answer table executed and reproduced, so neither the corpus SHA nor the AMG counts moved. All of this holds because identity is still decided by the newest quarter alone and history rides as a passenger. **Residual, deliberate:** 39,178 rows are zero today and nonzero historically; their history is recovered only where their identity survives into the current build. Letting historic counts vote on identity is exactly what would move the catalog, so under-recovery is the safe failure — closing it needs a resurrection policy with its own evidence, not a threshold tweak |
| **`junk?`'s `\A\d[A-Z]\b` rule is eating a DOOR COUNT catalog-wide.** **NUMBERS CORRECTED 2026-08-01 by the wave-6 APPLY** — the figures first filed here (8,236 keys / 46,842 vehicles; fi_traficom 44,932 · nl_rdw 9,594) came from the dossier and do not reproduce. Re-measured on pipeline `1134cd2` **at the exact string `junk?` receives** (the probed nameplate): **8,016 keys / 33,608 vehicles** match `\A[2-9]D\s`, and **100% of them die** every build; **6,157 keys / 26,705 vehicles** would pass if the door prefix were stripped (fi_traficom 24,818 · nl_rdw 2,198 · ca_nrcan 44 · nz_nzta 2). The recoverable figure is the one that matters and the two agree closely (26,705 vs 26,582); the gross figure was inflated because it was measured BEFORE the per-make family rules run — which is also why the original ranked Mercedes-Benz worst (7,567) when the true figure is 1,669: `mercedes()` resolves most `5D C 220 CDI`-shaped strings to a nameplate before `junk?` ever sees them. Worst keys: `5D Passat` 844 · `5D Golf` 498 · `4D S60` 427 · `5D Yaris` 373. By make (recoverable): Toyota 3,842 · Volvo 3,706 · Audi 3,068 · VW 2,833 · Ford 2,042. For **Škoda it is the dominant drop class** — 426 of its 614 junk-dropped vehicles (69%). Same class as the 115,809-vehicle rescue in data#141 | Kia+Škoda wave-6 dossier §E.8 F-1, re-measured and corrected in data#148 | NOT safely fixable blind, and the collision is sharper than first filed: the single largest matching key in the corpus is **`8D Audi A4` (1,381 vehicles)** — simultaneously the rule's own documented exemplar AND recoverable text — and **`4D Audi A8` (286)** is a door count and the D2 platform code *at the same time*, both pointing at the same car. Wants "strip a leading `[2-9]D ` only when the remainder resolves to a known nameplate for that make", a measured control build, and `report:junk_drops` confirming the recovery to the vehicle |
| **`junk?`'s `\A\d[A-Z]\b` rule is eating a DOOR COUNT catalog-wide — RE-MEASURED 2026-08-01, and the proposed remedy is REFUTED.** Own per-row replay: the clause kills **51,090 vehicles / 8,577 (make,key)**, which decomposes as door-shaped keys 33,609 (8,015 pairs) + bare body codes `4D`/`5D`/`2D` 13,178 (177) + real digit-letter nameplates and platform codes `8N`/`8H`/`1J`/`4L` 3,646 (198) + other 657. The three figures on record are three different denominators, all correct: **51,090** = what the clause kills; **47,286** = raw model strings starting with `[2-9]D ` (fi_traficom 45,380 · nl_rdw 1,903 — this is the earlier 46,842, drifted by newer renames); **26,707** = the key-level subset whose remainder is junk-free (the earlier 26,582). 47,286 − 33,609 ≈ 13,178: those rows reach `junk?` as a BARE `4D` because `collapse_variant` already ate ` SEDAN …`/` KOMBI …` — **the nameplate is destroyed before `junk?` ever sees the row, so no `junk?` edit can recover them.** ***`junk?` cannot fix ANY of this***: it returns a boolean, so relaxing it publishes the nameplate `5D Passat`, not `Passat` — 8,015 garbage records. The fix must strip the register's body/door prefix UPSTREAM of `collapse_variant` (fi_traficom `kaupallinenNimi` is formatted `<door><BODY> <model> <engine>-<typecode>/<wheelbase>`) | own replay 2026-08-01 (`report:junk_drops` method) + Kia+Škoda wave-6 dossier §pipeline-findings | Simulated the only fix shape that works (strip `\A[2-9]D\s` + optional body word from the RAW string, then re-classify): recovers 37,874 vehicles / 7,200 pairs, but only **579 pairs / 19,002 vehicles land on a live record** (Audi A4 2,702 · Mercedes C-Class 1,997 · E-Class 1,674 · VW Golf 1,403 · Passat 1,070) while **6,621 pairs / 18,872 vehicles MINT NEW NAMEPLATES** that are type-approval codes (`250D-124125/2800`, `V70 Stw 2.5D-SW7202/276`, `Felicia Combi 1.9D LX-EHH653/245`). Shipping it as measured would be the largest single garbage-minting event in the catalog's history. A future fix needs: (a) the door/platform separator, for which the corpus gives clean evidence — nl_rdw's Audi platform codes are ALWAYS followed by the MAKE word (`8D AUDI A4` 1,382, `4D AUDI A8` 286, `4D AUDI S8` 111, `8D AUDI S4` 91) while fi_traficom's door codes are followed by a body word or the model itself, and only 26 of the 8,015 door-shaped pairs involve nl_rdw at all; (b) a body-word vocabulary for the second token (SEDAN/KOMBI/HATCHBACK/ESTATE/STW/LIMOUSINE/CABRIOLET/SALOON/AVANT/MPV); (c) a rule for the type-code tail `-<code>/<wheelbase>`, which is what makes the 6,621; (d) a control build + `report:junk_drops` to the vehicle. Cheapest honest first slice: only rescue keys whose stripped remainder is ALREADY a live record for that make (579 pairs, 19,002 vehicles, zero new nameplates) |
| **Published popularity RANKS are unstable across enormous tie blocks.** `reconciler.rb attach_popularity!` ranks with `sort_by { -m["_counts"][cc] }` — Ruby's `sort_by` is not stable, and the tie blocks are the majority of the catalog: motorcycle/nl 4,298 of 5,070 ranked records sit on a tied count (largest block 830), car/nl 3,668 of 4,609 (largest 932). Measured against a control build 2026-08-01: adding **21 records** moved 11,346 records' popularity, changed **3,955 deciles** and shifted one rank by **926** — every move inside its own tie block, so no record's evidence changed, but the published decile of a tied record is effectively arbitrary and re-rolls on any insertion | own control build, junk2 "New" rescue PR | one-line: break ties deterministically (`sort_by { [-count, id] }`), which makes rank churn proportional to the actual change; then decide whether tied records should share a rank at all (competition ranking) rather than being ordered by id |
| The RDW comma-split hands Kia's internal PROJECT CODE the win over the nameplate (`ED,CEED` → Ceed loses; `PICANTO,SA` ×180; `SPORTAGE,SL,SLS`; `JB OD.RIO`), and `"CEE,D"` → `"Cee"` amputates a nameplate mid-word | Kia+Škoda wave-6 dossier; NAMING §7.4 row 2 already states the normalizer has this backwards | the wave papers over it with 12 rename keys; the durable fix is in the comma-split ordering, and NAMING's own table is the spec |
| `styling.yml:32` pins `XCEED: XCeed` commented "Kia official casing", but a rename silently undoes it — the pin is inert and its comment asserts the opposite of what ships | Kia+Škoda wave-6 dossier §E | one-line: either drop the dead pin or repoint the rename; a pin that documents a decision it does not enforce is worse than no pin |
| **One ruling needed across the catalog on MARKET-NAME pairs.** Two shipped passes disagree: the Ford wave used Wikipedia redirects as fold evidence, the Mazda wave kept Atenza/Mazda6 separate as a rebadge relation. Kia's `sedona`/`carnival` and `clarus`/`credos` sit on that fault line (sedona carries 7 countries). **Bentley's `continental-flying-spur` (2005–13, 6 countries) vs `flying-spur` (2013–, 10 countries) is the same pair one marque over** — Bentley itself dropped the Continental prefix at the generation change. **BYD makes this urgent and gives it a size (wave-8, 2026-08-01): the pairs are PUBLISHED ON BOTH SIDES, which none of the earlier cases were.** `car/byd/sealion-7` (8 countries) ↔ `car/byd/sea-lion-07` (es,ua); `car/byd/atto-3` (11 countries) ↔ `car/byd/yuan-plus` (es,ua); `car/byd/dolphin-surf` (5 countries) ↔ `car/byd/seagull` (nl,ua) — **three pairs where both ids publish today**; plus `car/byd/sealion-6`, `car/byd/seal-u`, `car/byd/seal-6` and `car/byd/atto-2` against unpublished `Song Plus` (303 ua), `Song Plus DM-i`, `Qin Plus` (60 ua) and `Yuan Up` (254 ua) = **7 pairs in one make**. **A naive one-to-one fold rule is not even WELL DEFINED here: `Song Plus` is the domestic name of BOTH the Sealion 6 AND the Seal U.** Behind them sits an unpublished Chinese-name tail of **22 further `ua_mvs`/`ar_dnrpa` strings, ~1,900 vehicles** (`Sea Lion 05/06`, `Song L`, `Song EV Plus`, `Qin`/`Qin L`, `Tang L`, `Han L`, `Xia`, `Shark 6`, `F-0`/`F-3`/`F-6`, `EV360`, `New F3`, `Seal 06`, `Seal 06GT` …) that publishes the day a second source picks any of them up. The wave-8 pass folded NOTHING here and invented no third answer | Ford wave-4 dossier vs Mazda wave-5 dossier vs Kia wave-6 dossier vs wave-7 Saab+Porsche+Bentley dossier §E.8 vs wave-8 Pontiac+Aston Martin+BYD dossier §A.3.4/§E.3 | owner or a single adjudicated rule; waves 6 and 7 both followed the Mazda posture to avoid a third improvisation. For the Bentley pair specifically, two routes already failed: bentleymotors.com's current model pages carry only the current name and no heritage timeline dating the drop, and neither id carries a TAN in the build, so `xrefs.tan` overlap (NAMING §2's strongest proof) returns nothing |
| **The `be_fps` licence gate fails on a STALE-CACHE FALLBACK, not a changing licence — and the pinned hash is CORRECT.** Blocked builds four times (a2b4e91, 1d32e74, and twice on 2026-08-01). **Corrected diagnosis (wave-8 apply, from the CI log):** the runner's fetch FAILS (`curl 404`), CI logs `fetch licenses: FAILED, keeping last-good cache (24575 bytes)`, and the gate then hashes that STALE artifact — which is where `e12304b9f486` comes from. Two consecutive live fetches reproduce the pinned `64fd989b565b` exactly, at 24,574 bytes (one byte apart from the stale copy). **My earlier entry here claimed the portal serves two variants and that the pin needed normalizing; that was wrong** — I diffed two *historical* pins and mistook a fetch-failure fallback for content churn. The page carrying no licence text at all is still true and still worth acting on separately | 2026-08-01 build failures on data#131, #132, #151, #157; corrected by the wave-8 apply's CI-log reading | **`rake licenses:pin` here is ACTIVELY HARMFUL** — it would overwrite a correct pin with the runner's stale artifact and make the failure permanent. Fix the FALLBACK instead: a licence fetch that fails must fail the gate loudly rather than silently hashing last-good cache, because a silent fallback turns a network blip into "the terms changed". Separately, the source page has no licence text, so the pin should record that fact rather than a CMS page hash |
| **The real BMW M1 (E26) has no id, and D-1 deliberately kept it that way.** `normalizer.rb#bmw` routes `M1` to `1 Series` via an explicit `/^M([17])\b/` line, because uk_dft's GenModel `BMW M1` is DfT's FAMILY LABEL, not the supercar. Verified against the cached `uk_veh0120_uk.csv` at the column the adapter actually reads (**2026 Q1 = index 6**; the header runs newest→oldest): the label holds **33,258 gb vehicles, of which 33,245 (99.96%) are M135i/M140i spellings** and the real car is `M1 COUPE` at **13 vehicles (0.04%)**. Minting `car/bmw/m1` from this rule would publish a 453-unit 1978 supercar id holding 33k hot hatchbacks. **CORRECTION 2026-08-01: an earlier version of this row claimed the dossier's figures "do not reproduce" and gave 3,755/3,744/11. That was MY error — I summed `cols.reverse.find { numeric }`, which is the LAST column, i.e. `2014 Q3`, an 11-year-stale quarter. The dossier's 33,245 of 33,258 is exactly right.** The lesson generalises past this row: VEH0120 has 53 columns — 6 metadata then 47 QUARTER columns running newest→oldest (2026 Q1 … 2014 Q3), of which 46 are historic — so any hand-rolled replay of that file must pin index 6 (or read the header) — picking a numeric column by position silently measures 2014 | `bmw-m-split.md` §6 DEBT trade; own replay of the DfT cache, data#163 | leave routed to `1 Series` until a second source attests the E26 independently of the family label — 13 single-source vehicles cannot clear any threshold anyway. If it is ever minted it needs a `removals.yml`-style ledger line explaining why an id exists for 13 vehicles |
| **`car/bmw/m6` and `car/bmw/m635csi` may be the same product under two market names, and D-1 mints the first while curation already keeps the second.** D-1 creates `car/bmw/m6` (2,196 vehicles, 9 countries) from the M6 badge; `renames.yml` separately keeps `car/bmw/m635csi` (72 vehicles, fi+nl) as a closed-badge KEEP with its own `former_ids` aliases. BMW sold the E24 as **M635CSi in Europe and as M6 in North America** — so on the market-name reading these are one car, and on the badge reading they are two. **This pass folded NOTHING here and invented no third answer**, exactly as waves 6-8 did for the same class | `bmw-m-split.md` §6 DEBT trade; the E24 badge split | RESOLVED IN MECHANISM 2026-08-02 — and the old text here was wrong twice, so it is restated rather than trimmed. It said this pair "awaits an owner ruling". (1) The FOLD question was already RULED and CLOSED: market-name pairs are TWO RECORDS, related, never folded. Nothing was ever waiting on that. (2) What was actually missing was a way to EXPRESS the relation, and that now exists — `pipeline#142` ships the mechanism (closed vocabulary, ten lint rules, private-by-gate) and `pipeline#143` migrates this pair with the other eight. So: KEEP BOTH, and the relation carries the fact. Do not resolve it by folding, then or now. The one live caveat is placement, not existence: §B6 rule 3 stores a symmetric row on the lexicographically smaller id, and `car/bmw/m6` sorts BEFORE `car/bmw/m635csi`, so the row lives on m6. Note the populations barely overlap in time: m6 is dominated by the 2005+ E63/F13, m635csi is 1984-89 |
| **Spaced M PERFORMANCE badges land on junk ids, and D-1 could not fix them without breaking curation.** `normalizer.rb#bmw` now de-spaces a leading `M <digit>`, but the lookahead is scoped to ONE digit — `(?=\d(?!\d))`. Widening it to all digits was implemented, measured and REVERTED: it rewrites `M 535 I` → `M535 I` and `M 635 CSI` → `M635 CSI` upstream of the rename layer, making `renames.yml`'s curated `"M 535 I": M535i` / `"M 635 Csi": M635CSi` keys inert and silently folding **12 + 72 curated vehicles** into `5 Series`/`6 Series`. So these remain on junk ids: `M 135` (1), `M 135 I` (1), `M 240I` (1), `M 340D` (1), `M 350I` (1), `M 440XI` (3), `M 535 I Automatic` (1), `M 535I` (1), `M 550 Xi` (1), `M 560` (1), `M 635 C51` (1), `M 635I` (1) — 14 vehicles, all single-source, none published. Partly filed already as gap G-4 | own measurement during the D-1 apply, data#161 / pipeline#96; `renames.yml` BMW block §"Performance M TRIMS split by a SPACE" | either extend the existing per-spelling rename keys (`M 135I`/`M 140I`/`M 550D` already do this correctly and are the cheap, safe route) or re-order so renames match BEFORE family rules — the second is a normalizer-wide change and must not ship while fold waves keyed on today's strings are in flight |
| **§C-3 the SPACE shape — sized by TWO independent derivations that disagree, and the obvious fix is REFUTED.** G-1's `TW_NAMEPLATE` reads a displacement; the merged designation rescue fires only on a HYPHEN. Names whose index follows a SPACE still fall onto the bare GenModel stub. **THE POOLED RESIDUE, measured the faithful way** — `two_wheeler_nameplate` replicated verbatim, keeping only rows that reach `count_fallback`, minus the `MODEL MISSING` rows the normalizer kills anyway — is **441,049 gb vehicles across 1,022 stubs in 215 makes** (2026 Q1, col idx 6). Restricted to the seven highest-volume makes it is 219,986 veh / 543 pairs, of which **176 pairs (127,714 veh) already match a live id and 134 rows / 93,290 veh route onto ids that EXIST but LACK `gb` — that subset is the actionable prize.** **CORRECTION 2026-08-02: an earlier version of this row said "87,302 vehicles across 75 stubs in 38 makes". That number is real but it is a DIFFERENT QUANTITY — my simulation incremented its counter only inside the branch where my candidate rule fired, so it measured the RULE'S REACH, not the problem's size, and the row described it as the latter.** Caught by a second, independent derivation. **The obvious fix stays REFUTED**: fusing stub + designation produces `BONNEVILLE-T120`, `XJ-6`, `TRACER-9`, while the live catalogue publishes `yamaha/xj6` as **"XJ6"** (fused), `yamaha/yzf-r125` as **"YZF-R125"** (hyphen) and `yamaha/tracer-9` as **"Tracer 9"** (space) — three join characters inside one make | own simulation + independent replay of `uk_dft.rb`'s adapter path, 2026-08-02; yamaha dossier §C-3 | **A ROUTING problem, not a naming one** — the targets already exist, so it is curation (per-family rename keys, join char READ from the live record, never derived), and the 134-row subset is the priority order. Three traps recorded: (a) the dominant NONE shape is **E5/E4/ABS/model-year suffixes** — `YZF R1` alone has 15 model-year rows totalling ~7.5k veh, and none of those suffixes is identity; (b) `TRIUMPH BONNEVILLE T 100 (865)` (4,546 veh) normalises onto `bonneville-t100` but **`triumph/bonneville-865` is live** — routing it silently merges two generations, so it needs judgement not a match; (c) `yamaha/r` and `yamaha/x` are SINGLE-LETTER stubs pooling RayZR (1,378) and X-MAX Tech Max 125 (824) — neither is a nameplate, and eleven stubs would EMPTY OUT entirely if routed, retiring the id |
| **NEW SILENT-FAILURE CLASS: RENAME-VALUE LIVENESS. `lint_curation` checks that a rename's KEY is reachable; NOTHING checks that its VALUE still names a live record.** `renames.yml` maps display name → display name and is applied in ONE PASS. So when a fold retires an id, a PRE-EXISTING rename whose **value** is that retired display name keeps firing, resolves to the dead display, is never re-mapped onto the survivor, and **resurrects the retired id under a live key — green lint, green build, duplicate back in the catalogue.** This is distinct from the alias-chain class (which MISROUTES and is already guarded) and from the direction-war class (which the lint does catch). First-pass sweep of all **7,363** string-valued renames against a live build: **466 values name no live record**, of which **34 point at a slug that carries a `former_ids` arm** — the resurrection shape. **CONFIRMED instance: the Austin Healey cluster.** `car/austin/healey-3000`, `/healey-100`, `/healey-100-6` and `/healey-sprite` are all retired (each is a `former_ids` KEY, aliased into the `austin-healey` make) and none is live in the build — yet ~17 keys in the `Austin:` block still resolve to `"Healey 3000"`, `"Healey 100"`, `"Healey 100-6"` and `"Healey Sprite"`. **BOUNDS, stated honestly: I have NOT traced whether these actually republish** — the Austin→Austin Healey make MOVE may fire first and take the rows with it, and the remaining 432 "dead value" hits are mostly benign (fold targets that simply do not clear threshold, and self-referential display pins where key == value). So 34 is a CANDIDATE list, not a defect list | discovered by the honda dossier's own §A-2W analysis (it found exactly two instances in its own scope, `renames.yml:3696-3697`, where `Goldwing GL1500`/`GL1800` would resurrect the ids the Gold Wing fold retires); generalised and swept 2026-08-02 | TWO actions: (1) add the check to `scripts/lint_curation.rb` — for every string-valued rename, assert the value's id is live OR is not a `former_ids` key; ship it REPORT-ONLY first, because 466 hits need triage before any of them can block a build; (2) adopt the rule for every fold from now on, in the honda dossier's words: **"for every id a batch retires, grep renames.yml for that id's display name as a VALUE, not just as a key."** The yamaha and suzuki applies shipped tonight were checked this way after the fact and are clean — every fold target in both is live |
| **THE TMAX NAMING IS WRONG IN EVERY MARKET, and four ids now carry one nameplate. Owner decision needed; I am not re-spelling four published ids unilaterally.** Audit S-4 (`review-yamaha-apply.md`), confirmed. Yamaha EU titles the machine `TMAX` / `TMAX SX` / `TMAX DX` / `TMAX Tech MAX` from 2010 to 2026 — **never with a displacement**; the current PDP is `/pdp/tmax/` with `"title":"TMAX"` and the string "560" does not appear on it. Japan is the only market that uses displacements and writes them **CLOSED**: `TMAX530`, `TMAX560 TECH MAX ABS`. **So the spaced form this catalogue uses — "TMAX 500", "TMAX 530", "TMAX 560" — exists nowhere, and "TMAX 500" has never existed in any market in any spelling.** Two consequences beyond spelling: (a) **`XP500` spans two displacements** — the 59C (2012) and 2PW (2015-16) manuals print `XP500`/`XP500A` against **530 cm³** while only the 5VU is 499 cm³, so folding `XP500` [es,fi,gb,lu,nl,nz,ua], the largest single fold in that batch, onto "TMAX 500" merges two engine generations under a number wrong for one of them; (b) the catalogue now carries **FOUR ids for one EU nameplate** — `tmax` (d4, [gb,nl,nz,ua]), `tmax-500`, `tmax-530`, `tmax-560` — where Yamaha Europe has one nameplate and three trim names. `tmax-500` predates my change; `tmax-530` and `tmax-560` were MINTED by it and published in 2026.08.2, which is what makes this urgent rather than theoretical: minting is what makes a naming decision permanent | audit S-4, confirmed against Yamaha EU PDP + JP lineup + manual covers, 2026-08-02 | THREE options, and the choice is the owner's because each has a different cost. **(1) Close the spacing** — `TMAX530`/`TMAX560` per Japan, re-slugs two ids I just minted, needs former_ids and is the smallest defensible move. **(2) Collapse to one nameplate** — everything to `tmax` with SX/DX/Tech Max as variants, matching Yamaha Europe exactly; largest diff, retires three ids including one published long before me. **(3) Keep as-is and document** — cheapest, but leaves a spelling no market uses on four ids. I lean (1) then (2) as a separate pass, because (2) is the market-name class the BYD/Bentley/Kia ruling already owns and should not be decided ad hoc here. **What must NOT happen is a fourth improvisation.** Blocking detail for whoever takes it: `XP500`'s two-displacement span means option (2) is not merely cosmetic — it is also the only option that stops asserting 499 cm³ for 530 cm³ machines |
| **UK 2W nameplates whose numeral is a SINGLE DIGIT fall through to the bare series stub — 22,805 gb vehicles, 349 rows, and it is why the whole Tracer 7 / Tracer 9 fleet is invisible.** `uk_dft.rb`'s `TW_NAMEPLATE` needs **2-4 digits** (`\d{2,4}`) and its designation rescue `TW_DESIGNATION` needs **a hyphen group**. "TRACER 7 GT (MTT 690D)" has neither, so it takes `count_fallback` to the GenModel stub. The register is not thin here — it prints the commercial name in full, better than any other source we have: `TRACER 7 (MTT 690-A)`, `TRACER 7 GT Y-AMT (MTT690D-S)`, `TRACER 9 GT+ (MTT890D-K)`. We throw all of it away at SOURCE altitude, where no rename, alias or move downstream can recover it — the same shape as the truncation pipeline#138 fixed. **Measured on 2026 Q1, column index 6:** 4,524 gb vehicles on `yamaha/tracer` (= every TRACER 7/7 GT/9/9 GT/9 GT+ row, summing to that figure exactly), then YAMAHA XJ 3,926 · BETA EVO 3,874 · PEUGEOT SPEEDFIGHT 2,470 · TRIUMPH ROCKET 2,216 · SYM JET 1,459 · YAMAHA FAZER 723 · DUCATI ST 329. Most of those ARE real nameplates — Rocket 3, Speedfight 3/4, ST2/ST3/ST4, Jet 4, XJ6 — so this is not a junk class | measured with `scratchpad/fallback_scan.rb` replaying the CSV the way `uk_dft` reads it, and confirmed by replaying the real adapter + Normalizer: uk_dft aggregates ALL Yamaha Tracer rows into just four raws, one of which is "YAMAHA TRACER" n=4,524, 2026-08-02 | A pipeline change, not a curation one, and it needs its own control build because the blast radius crosses eight makes. Sketch: allow a single digit when the Model column's family CONTAINS the GenModel stub (the containment guard already written for designations), so "TRACER 7" is admitted under stub "TRACER" while nothing that resolves today can move. **Do not widen `\d{2,4}` to `\d{1,4}` globally** — that reads "CBR 6" out of "CBR 600" ranges and re-opens the capacity path this rule was built to protect. The 2W curation blocks (Kawasaki, Honda 2W) should be re-derived AFTER it lands, for the reason G-1 already taught: a fix changes what is detectable |
| **RESOLVED 2026-08-02 — "Tracer 9GT Plus".** (Kept as a record of how it was decided.) `Support.slugify` drops `+`, so the printed name cannot be the id name: "Tracer 9GT+" slugifies to `tracer-9gt` and collides with the published GT — and that is not theoretical, RDW`s "TRACER9 GT+" / "TRACER 9 GT+" already produce "Tracer 9GT+" and are ALREADY merged into `tracer-9gt`, silently. Both options I leaned toward are closed by measurement. **(1) slugify `+` -> `plus`: KILLED — 27 published records carry `+` in their name** ("260Z 2+2", "365 GT4 2+2", "Elan +2", "XG+530", "Agility 16+ 125", four Trek Allant+ e-bikes, …) and every one re-slugs; 27 ids damaged to represent 1. **(2) normalise the GT family to spaced: KILLED — glued IS the house rule**, produced by `Normalizer#two_wheeler_spacing`, which collapses letter/digit whitespace and re-splits only where a 4-LETTER run touches the boundary ("TRACER"(6) splits, "GT"(2) does not). It applies across every 2W make — "NINJA 1000 SX" -> "Ninja 1000SX", "MT 09 SP" -> "MT-09SP" — and carries a DO NOT REVERT history note. What survives is Yamaha`s own URL name spelled to the house convention: **"Tracer 9GT Plus" -> `tracer-9gt-plus`**, distinct, no collision, nothing retired | slugify blast radius measured over all 13,826 published records; the spacing rule read at `normalizer.rb` `two_wheeler_spacing`; Yamaha PDP `/sport-touring/tracer-9-gt-plus/`; UK VEH0120 "TRACER 9 GT+ (MTT890D-K)" n=737, 2026-08-02 | Shipped with the same change. **It also corrected a defect data#263 shipped hours earlier**: that PR spelled the new id "Tracer 7 GT" (spaced) and justified it by calling the glued names accidents. They are not — and the spaced spelling was a nameplate SPLIT waiting to happen, because "TRACER 7 GT" produces "Tracer 7GT" -> `tracer-7gt` while the type codes were being sent to `tracer-7-gt`. Caught before release, so nothing is owed to consumers |
| **Four Yamaha type codes still publish as if they were nameplates, and they are now the only ones left.** `mtt890d-k`, `mtt890d-sk` (blocked on the `+` row above), plus two that predate all of this and were never in any dossier's scope: `motorcycle/yamaha/mtm690d-u` [es,nl] and `motorcycle/yamaha/mtn850d` [fi,nl]. Also `motorcycle/yamaha/mtt850` [fi,nl] — MTT850 is the Tracer 900's code, and `tracer-900` is live [gb,nl,nz,th], so that one is a plain fold nobody has written | build/out after data#248 + this change, 2026-08-02 | `MTM690D` is already mapped to XSR700 in the renames block (library.ymcapps.net "XSR700 LEGACY - MTM690D"), so `MTM690D-U` is a one-line addition — the `-U` sibling was simply missed. `MTN850D` is the MT-09 SP's 2014-2020 code; check it against `mt-09sp` before folding, because the earlier SP is arguably a different machine. `MTT850` -> "Tracer 900" needs the flatten-both check first |
| **Kawasaki spells it "Versys-X 300" in three markets and the pipeline cannot produce that string.** `classify("KAWASAKI", "VERSYS-X 300")` returns **"Versys-X300"**: `Normalizer#two_wheeler_spacing` collapses every letter/digit boundary and pass 2 re-splits only where a **4-LETTER run** touches it — "VERSYS-X" ends in a lone `X`, so nothing splits. The kawasaki dossier §A-23 proposed re-keying `versys-x300` + `versys-x300abs` to the spaced form; the pre-flight rejected it and only the ABS half shipped. This is the SAME rule that made "Tracer 7 GT" unshippable (data#263 -> data#267), so it is now twice in one week and it is not a Yamaha quirk: any 2W nameplate whose family ends in a short token cannot carry a space before its number, whatever the manufacturer prints | US og:title "Kawasaki **Versys®-X 300** | Touring Motorcycle"; AU og:title "Kawasaki **Versys-X 300**"; parts `/parts/2026/KLE300CTFAL` -> "2026 **VERSYS®-X 300** ABS"; EU accessory "Centre Stand **Versys-X 300**". Rule read at `normalizer.rb` `two_wheeler_spacing`, 2026-08-03 | A RULE-level question, not a per-key one, and it should be answered once for the whole 2W corpus rather than per make. Two shapes: **(a)** leave it — the glued form is consistent, self-documenting and already published, and the cost is that a handful of ids disagree with the manufacturer`s spacing; **(b)** widen pass 2`s re-split so a family ending in a HYPHEN + single letter also splits ("VERSYS-X" -> "Versys-X 300"), which is a one-line change with a corpus-wide blast radius that must be measured before it is written — the same measurement that killed the `+`->`plus` slugify idea (27 ids damaged to fix 1). **Do not fix it per-key in renames.yml**: a rename VALUE the pipeline will not produce splits the nameplate instead of correcting it |
| **SEVEN Vespa nameplates are split across two makes right now, and no gate will ever tell you.** The id-contract move-split gate fires only when BOTH halves of a badge-prefixed/badge-free pair fall below threshold — it reports INCIDENTS, not EXPOSURE. Sweeping all 47 `Piaggio\|Vespa *` moves for a badge-free twin with no co-move found **nine**: two were the incidents that had main red (`lxv125`, `px150e`, fixed in the same change as this row) and **seven are LIVE under `piaggio/` while their badged spelling sits under `vespa/`** — `piaggio/50` (candidate, nl 3), `piaggio/50s` [nl,nz], `piaggio/et2` [gb], `piaggio/lx50` [fi,nl,nz], `piaggio/gts125` [es,fi,nl], `piaggio/px125e` [gb,lu,nl], `piaggio/px200e` [lu,nl,nz]. Each is one nameplate publishing as two records under two different makes, with every instrument green, because each half independently clears threshold | swept with `scratchpad/vespasweep.rb` — reads moves.yml, derives each badge-free twin`s id, and checks it against build/out and build/candidates; keys verified with `classify()` rather than string surgery, 2026-08-18 | Deliberately NOT bundled into the red-main fix: these retire **seven published ids** and need their own reviewed change with former_ids arms and a country-loss check, for the same reason A-21 did not belong inside the kawasaki apply. The precedent is already adjudicated (`Vespa 50`, and "Vespa Cosa: your read is approved … Piaggio is yours"), so this is an apply, not a question. **S2W is claiming it** — next after main is green. The general lesson is the one worth keeping: **a gate that fires on a threshold reports a subset of its own defect class, and the subset is the accident of where the mass fell.** Worth asking of every gate we have: what does it stay silent about when the numbers are large? |
| **A ROLLING-WINDOW SOURCE MAKES TYPE APPROVALS EXPIRE, and the xref-loss gate turns that into a fold`s fault.** `lu_snca` serves a rolling THREE-MONTH window. Four Kawasaki `tan` values were real when release 2026.08.x published them, appear in exactly one monthly delta (April 2026), are carried by NO other source, and are simply gone now that the window reads 2026-05…2026-07. The xref-loss gate correctly reports them leaving the catalog — and correctly attributes them to the alias, because the alias is where a consumer would look. But **re-cutting the fold cannot recover them**: an unfolded `z800abs` rebuilds from the same absent rows. So the only available remedy is a sign-off, and that means **every future fold of an LU-sourced id will need one**, forever, for evidence that was genuine when we shipped it. Three signed off in this change (the first uses of `accepted_xref_loss:`); the fourth, fifth and hundredth will be noise | measured four ways: (1) `grep -rlF` over the whole cache tree returns only lu_snca files for all four values; (2) within LU they are in April`s delta alone — May/June/July carry zero; (3) the publishing release ran `lu 2026-04…2026-06`, today`s build derives `lu 2026-05…2026-07`; (4) the freshly fetched lu_delta_202607.xml (39 MB, 2026-08-18) carries none. The gate`s own "leave the CATALOG entirely" diagnosis proves no live record holds them under any id, 2026-08-18 | THE QUESTION FOR THE OWNER: type approvals are the cross-source join key (PRD-3 D7), so should they **ACCUMULATE across releases** the way `archive/registrations` accumulates snapshots, rather than being recomputed from whatever window a source happens to serve today? An approval is a durable fact about a machine — it does not stop existing because a registrations feed rolled forward. Sketch: an `archive/xrefs` store keyed by id, unioned into each build, with provenance (`first_seen` release) so a value can never silently vanish; the xref-loss gate then only fires on a genuine re-cut. Cost is a new durable store and a decision about pruning. **Until this is ruled, do not read the three sign-offs as precedent** — they carry their measurement precisely so the next reader can tell an upstream expiry from a careless fold |
| **2W POWERTRAIN VERIFICATION: PASSED. Block 8 closes, and the four surprising entries all have real explanations — recorded so nobody re-investigates them.** Coverage first, because it was not written down anywhere: `by_powertrain` lives in the PRIVATE layer inside each record`s `registrations` block (not a top-level `powertrain` field, and not in `registrations-*.json` — I looked in both wrong places first). **motorcycle 3,349/5,901 = 56.8%; moped 662/1,340 = 49.4%; car 3,765/5,128 = 73.4%.** So the 2W powertrain gap is real but far smaller than the 2W ENRICH gap (3.8% vs 27.6%) — different fields, and the enrich one is the bigger hole. Vocabulary in use: motorcycle `ice-petrol` 3254, `bev` 95, `hev` 2, `phev` 1, `ice-diesel` 1; moped `ice-petrol` 420, `bev` 191, `ice-diesel` 58. **THE FOUR EDGE CASES, each checked against the records rather than reasoned about:** (a) **58 DIESEL MOPEDS is CORRECT** — they are Aixam, Bellier, Casalini, Ligier light quadricycles (L6e/L7e), genuinely diesel, and this catalogue maps that class to the `moped` kind; 2,151 of the 2,166 vehicles are `fi`, where licence-free quadricycles are popular. I filed this as "essentially impossible" in my own head before reading the ids. (b) **`piaggio/ape-classic` ice-diesel** — the Ape is a diesel three-wheeler, classified `motorcycle`. Correct. (c) **`kawasaki/ninja-7-hybrid` + `z7-hybrid` hev** — real machines, the first mass-production hybrid motorcycles (2024), and a good sign the pipeline picks up new drivetrains without help. (d) **`honda/gl1800bd` phev es:1** — the only genuine oddity: a Gold Wing is not a plug-in hybrid, so this is one mis-coded es_dgt row | read from `build/out-private/catalog-plus/{motorcycle,moped,car}/models.json` on the synced pipeline (origin/main f8a8279), 2026-08-22 | **No code change proposed, deliberately.** `by_powertrain` is defined as what registers OBSERVED (emit.rb, spec-ev-powertrain §1.2), so (d) is an accurate observation of an inaccurate register row, not a defect in us — suppressing it would substitute our judgement for the evidence, which is the opposite of the field`s contract. If a threshold is ever wanted for observational fields, that is a spec question and n=1 is the case that motivates it. **The durable value here is (a):** the next person to look at "diesel mopeds" will have the same reaction I did, and the answer is one sentence long |
